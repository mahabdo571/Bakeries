name: testproj
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0


dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  http: ^1.3.0
  json_annotation: ^4.9.0
  shared_preferences: ^2.4.0
  path_provider: ^2.1.5
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/json/dashboard.json
    - assets/json/products.json
    - assets/json/purchases.json
    - assets/json/production.json
    - assets/json/users.json
    - assets/json/settings.json

  
