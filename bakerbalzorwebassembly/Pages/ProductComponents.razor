@page "/productComponents"
@using Business.Shared.DTOs
@using Business.Shared.Enums
@using bakerbalzorwebassembly.Component.ProductComponents
@using bakerbalzorwebassembly.Component.Shared
@using bakerbalzorwebassembly.Models
@inherits ComponentBase
<PageTitle>مكونات المنتج @navigationMode.productDTO!.Name</PageTitle>
<h3 class="mb-4">
    <i class="bi bi-inboxes"></i> مكونات المنتج @navigationMode.productDTO!.Name
</h3>

<PageToolbar SearchText="@searchText"
             OnSearch="@Search"
             OnAdd="ShowAddModal"
             ShowButtonAddNew="@true" />


<ProductComponentsTable Items="@filteredData"
                        OnDetails="ShowDetails"
                        OnEdit="ShowEdit"
                        OnDelete="ShowDelete" />


<DetailsModal Item="@selectedForDetails"
              OnClose="CloseDetails" />

<DeleteModal Item="@selectedForDelete"
             OnConfirm="ConfirmDelete"
             OnClose="CloseDeleteModal" />


<EditModal Item="@selectedForEdit"
           OnSave="@SavePurchases"
           CurrntProduct="@navigationMode.productDTO"
           OnClose="@CloseEditModal"
           StockModel="@StockModel" />

<LoadingModal IsVisible="@isSaving" />

<ErrorModal Message="@messageError"
            OnClose="CloseDeleteModal" />